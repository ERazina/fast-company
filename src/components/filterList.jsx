import React from "react";

export const FilterList = (props) => {
  console.log(props);
  return (
    <ul className="list-group">
      {/* {professions.map((profession) => {
        // <li className="list-group-item">{profession.name}</li>;
      })} */}
    </ul>
  );
};
